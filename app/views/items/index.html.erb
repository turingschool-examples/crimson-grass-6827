<h1> All Items </h1>

<% @items.each do |item| %>
  <section id="item-<%= item.id %>">
<%= " #{item.name} | $#{item.price} | At: #{item.supermarket.name} | #{item.count_customers} person/people have bought this item" %> 
  </section> </br>
<% end %>